export type Developer = {
  developerId: number;
  githubUsername: number;
  telegramHandle: number;
  xHandle: number;
};

export type Client = {
  clientId: number;
  developerId: number;
  gameId: number;
  name: number;
  url: number;
};
